spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bettracker
    username: bettracker
    password: bettracker123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect


server:
  port: 8080

jwt:
  secret: bettracker2026SuperSecretKeyPourSignerLesTokensJWT
  expiration: 604800000 #7 jours
